import React, { Component } from 'react';
import Header from './Header';
// import { dimensions } from '../base_styles';
// var recievedMessage = this.props.state.cart

export default class Cart extends Component {
    render() {

        return (
            <div>
                <Header/>
            </div>
        );
    }
}